package code;

import annotations.A1PD;
import annotations.AMPD;
import annotations.ASP;
import annotations.UneAnnotation;
import java.util.ArrayList;
import java.util.List;

/*
 *
 * @author yvan
 */
/*
@ASP
public class UneClasse<@ASP T> {
    private T t;
    @A1PD("d'un attribut")
    public @AMPD int a;
    @AMPD(
    value=1,
    un = 2,
    deux = 3)
    @UneAnnotation(1)
    @UneAnnotation(2)
    @UneAnnotation(
            unDouble = 2.0,
            uneChaine = "trois"
    )
    public <@ASP T> void uneMethode() {
        List<@ASP T> l = new ArrayList<>();
    }
}*/
public class UneClasse {
    
}
